# Backend de Sinalização WebRTC

Servidor Node.js usando WebSocket para sinalização entre peers WebRTC.

## Como rodar

```powershell
npm install
npm start
```

O servidor escuta na porta 8080 por padrão.
